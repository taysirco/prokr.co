import { neomContext } from './context';
import { neomCompanies } from './companies';
import { neomContent } from './content';

export const neom = neomContext;
export { neomCompanies, neomContent };
