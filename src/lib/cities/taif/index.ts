import { taifContext } from './context';
import { taifCompanies } from './companies';
import { taifContent } from './content';

export const taif = taifContext;
export { taifCompanies, taifContent };
